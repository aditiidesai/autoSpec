# --- Core Framework ---
streamlit==1.32.2

# --- LLM + OpenAI ---
openai==2.8.1
tiktoken==0.7.0
python-dotenv==1.0.1

# --- Vector DB --- because telemery arg number mismatch
chromadb==1.3.5

# --- Utilities ---
pydantic==2.7.0
requests==2.31.0

# --- Force NumPy < 2.0 because ChromaDB breaks with np.float_ ---
numpy==1.26.4

# --- Optional helpers ---
PyYAML==6.0.1
